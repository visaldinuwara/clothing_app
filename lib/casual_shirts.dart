import 'package:flutter/material.dart';

class CasualShirts extends StatelessWidget {
  const CasualShirts({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Casual Shirts')));
  }
}
