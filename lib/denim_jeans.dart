import 'package:flutter/material.dart';

class DenimJeans extends StatelessWidget{
  const DenimJeans({super.key});
  @override
  Widget build(BuildContext context) {
        return Scaffold(
      appBar: AppBar(title: const Text('DenimJeans')),
      body: const Center(
        child: Text('DenimJeans.'),
      ),
    );
  }
}